// // middleware/localStorageMiddleware.js

// const localStorageMiddleware = store => next => action => {
//     if (action.type === 'ADD_GROUP') {
//       // Update local storage with the new value
//       localStorage.setItem('myValue', JSON.stringify(action.payload));
//     }
//     return next(action);
//   };
  
//   export default localStorageMiddleware;
  